import 'package:flutter/material.dart';

class Jordans extends StatelessWidget {
  static const routeName = '/jordan';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
